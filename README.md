# React + Vite

## 快速开始

```bash
npm install
npm run dev
```

构建：

```bash
npm run build
```

## 目录速览

- `src/App.jsx`：应用入口组件，包含计数器与表格示例。
- `src/components/HelloWorld.jsx`：展示个人信息与按钮交互。

项目已完全移除 i18n 逻辑，保持最小的 React 示例结构，便于继续扩展业务代码。
